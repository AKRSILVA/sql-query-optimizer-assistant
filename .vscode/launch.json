{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Rodar teste de conexao",
            "type": "python",
            "request": "launch",
            "module": "database.test_connection",
            "cwd": "${workspaceFolder}"
        }
    ]
}